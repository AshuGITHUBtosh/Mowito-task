## Camera Toggle
This project implements a ROS2 package for image conversion using a live camera feed.
The node subscribes to images from the usb_cam package, converts them to grayscale or color based on a service call, and publishes the converted images to a new ROS2 topic.

The system also displays the processed image using OpenCV for real-time visualization.




## Tools used
 - ROS2 Humble
 - OpenCV
 - cv_bridge
 - usb_cam

## Dependencies & Installation
### Install ROS2 on Ubuntu
- ```sudo apt install ros-humble-desktop```
- ```source /opt/ros/humble/setup.bash```
### Install Python Dependencies
- ```pip install opencv-python numpy scipy```

### Install ROS2 Packages
- ```sudo apt update && sudo apt install ros-humble-vision-msgs ros-humble-cv-bridge python3-numpy```

 ## Execution
 - ``` colcon build ```
 - ``` source install/setup.bash```
 - ```ros2 launch image_conversion_pkg image_conversion_launch.py ```
 - ``` ros2 service call /set_grayscale_mode std_srvs/srv/SetBool "{data: true}"```
 - ``` ros2 service call /set_grayscale_mode std_srvs/srv/SetBool "{data: flase}"```



# Behavior Tree Task – Robot Room Navigation

## Overview

This task demonstrates the use of a **Behavior Tree** to model a simple robot task flow using the **BehaviorTree.CPP** library.
The robot performs a sequence of actions to:

1. Move to a room door
2. Check if the door is closed
3. Open the door if needed
4. Enter the room
5. Move to the fridge
6. Check if the fridge door is closed
7. Open the fridge
8. Find an apple
9. Pick the apple
10. Close the fridge
11. Move back to the room door
12. Exit the room



# Behavior Tree Structure

The behavior tree uses the following control nodes:

### Sequence

A **Sequence** executes children in order and fails if any child fails.

### Fallback

A **Fallback** node tries children until one succeeds.

Example logic used in the tree:

```
Sequence
 ├── MoveToRoomDoor
 ├── Fallback
 │    ├── IsDoorClosed
 │    └── OpenDoor
 ├── EnterRoom
 ├── MoveToFridge
 ├── Fallback
 │    ├── IsFridgeClosed
 │    └── OpenFridge
 ├── FindApple
 ├── PickApple
 ├── CloseFridge
 ├── MoveToRoomDoor
 └── ExitRoom
```

---

# Project Structure

```
bt_task/
│
├── main.cpp
├── CMakeLists.txt
└── build/
```

* **main.cpp** – Contains all behavior tree node implementations and the tree definition
* **CMakeLists.txt** – Build configuration using CMake
* **build/** – Build directory generated by CMake

---

# Dependencies

Install the BehaviorTree library:

```bash
sudo apt install ros-humble-behaviortree-cpp
```

This installs the **BehaviorTree.CPP** framework used to implement the tree.

---

# Build Instructions

Navigate to the project directory:

```bash
cd bt_task
```

Create a build folder:

```bash
mkdir build
cd build
```

Run CMake:

```bash
cmake ..
```

Compile the program:

```bash
make
```

If compilation succeeds, an executable named **bt** will be created.

---

# Running the Program

Run the executable:

```bash
./bt
```

---

# Expected Output

Example terminal output:

```
Moving to room door
Checking if door is closed
Opening door
Entering room
Moving to fridge
Checking if fridge door is closed
Opening fridge
Finding apple
Picking apple
Closing fridge
Moving to room door
Exiting room
```

# Forward Kinematics of a Planar Robot Arm
# Description

This program implements Forward Kinematics for a planar robotic arm.

The program:

Calculates the position of each joint

Computes the final end-effector position

Visualizes the robot arm using Matplotlib


## How to Run

- ``` cd camera_ws ```
- ``` python3 forward_kinematics_visualized.py ```


   
 
